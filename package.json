{
  "name": "web-metrics",
  "version": "0.1.0",
  "description": "Measure web vitals and request timing for a set of URLs",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "measure": "node src/index.js config.json"
  },
  "dependencies": {
    "playwright": "^1.50.0",
    "speedline-core": "*"
  }
}
